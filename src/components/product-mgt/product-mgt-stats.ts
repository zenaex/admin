import type { ProductMgtStats } from "@/components/product-mgt/product-mgt-types";

/** UI shell stats — replace with API-derived values when wired. */
export const PRODUCT_MGT_STATS: ProductMgtStats = {
  totalProducts: "₦ 150,000",
  activeProducts: "100,000",
  totalCrypto: "10",
  totalCurrencies: "4",
};
